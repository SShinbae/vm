/**
 * @deprecated This file is maintained for backward compatibility.
 * Import from '@/src/config' for new code.
 */

// Re-export from new location
export { queryClient, queryClientConfig, queryKeys } from "@/src/config";
